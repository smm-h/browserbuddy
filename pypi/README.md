# browserbuddy

This PyPI package is a name reservation for BrowserBuddy, the browser/AI shared-space MCP server.

The actual server ships on npm: `npx browserbuddy serve` — see https://github.com/smm-h/browserbuddy
